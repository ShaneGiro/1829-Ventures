# ─────────────────────────────────────────────────────────────────────────────
# 1829 Ventures CRM — Python dependencies
#
# IMPORTANT — install CPU-only PyTorch BEFORE running pip install -r:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Then install everything else:
#   pip install -r requirements.txt
#
# PyTorch is required by sentence-transformers (embedding model). Without the
# cpu-only flag pip will pull the CUDA build (~2 GB), which is unnecessary on
# the dev VM and will exhaust the 4 GB RAM budget.
# ─────────────────────────────────────────────────────────────────────────────


# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0        # standard = includes websockets + httptools
pydantic>=2.10.0
pydantic-settings>=2.7.0         # env-var settings via BaseSettings
python-multipart>=0.0.19         # required for file upload endpoints


# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0                  # async Postgres driver used by FastAPI
psycopg2-binary>=2.9.10          # sync driver used by Alembic migration scripts
geoalchemy2>=0.15.0              # PostGIS geometry column types
pgvector>=0.3.6                  # pgvector column type + HNSW index helpers


# ─── Background Tasks ─────────────────────────────────────────────────────────
celery>=5.4.0
redis>=5.2.0                     # Celery broker + result backend


# ─── Authentication ───────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0 # JWT signing and verification
authlib>=1.3.2                   # Google OAuth 2.0 client
httpx>=0.28.0                    # async HTTP client (OAuth flow + test client)


# ─── Object Storage ───────────────────────────────────────────────────────────
boto3>=1.35.0                    # S3-compatible interface — MinIO in v1, R2 in v2


# ─── Email ────────────────────────────────────────────────────────────────────
sendgrid>=6.11.0                 # transactional email via SendGrid SMTP relay


# ─── AI / Embeddings ──────────────────────────────────────────────────────────
# See note at top of file — install torch CPU build first.
sentence-transformers>=3.3.0     # all-MiniLM-L6-v2 (80 MB) for pgvector embeddings


# ─── MCP Server ───────────────────────────────────────────────────────────────
mcp>=1.2.0                       # Anthropic MCP SDK — SSE server transport for kernelbot


# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1             # load .env files in local development


# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0           # async test support
pytest-cov>=6.0.0                # coverage reports
factory-boy>=3.3.1               # model factories for test fixtures


# ─── Linting & Type Checking ──────────────────────────────────────────────────
ruff>=0.8.0                      # linter + formatter (replaces flake8, isort, black)
mypy>=1.13.0                     # static type checker
types-redis>=4.6.0               # mypy stubs for redis
